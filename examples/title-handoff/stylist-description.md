Create one clean, publication-ready scientific methodology diagram showing the inference workflow for evidence-grounded answer generation. Use a landscape canvas of approximately 2000 × 1000 pixels, with a 2:1 aspect ratio, a pure white background, generous whitespace, and a clearly readable left-to-right flow. Keep the supplied figure caption and any overall figure title outside the artwork. Do not render a figure title or caption anywhere in the image. Component labels, the retrieval-group heading, arrow labels, and the meaningful legend described below belong inside the artwork.

Preserve a single main row with seven sequential visual elements: a user question, a frozen question encoder, a query vector, a nearest-neighbor retriever, exactly five retrieved passages, a generator, and an answer with passage citations. Add a direct path from the original question to the generator above the main row. Place the read-only document index below the retriever and connect it upward into the retriever. Every arrow represents inference-time information flow and points toward its recipient. There are no training or parameter-update flows.

Center the main row at approximately 47% of the canvas height. Reserve a clear upper lane near 20% of the canvas height for the original-question bypass. Use the lower third for the document index and a compact legend, leaving comfortable whitespace beneath them. Keep a consistent outer margin, align process blocks on their horizontal centerline, and balance spacing by visual weight. Use these approximate horizontal locations as a starting point: question at 5–14% of the width, encoder at 19–31%, vector at 34–40%, retriever at 44–56%, passage cards at 60–66%, generator at 71–82%, and answer at 87–98%. Adjust these positions and widths modestly to maintain the outer margin and readable labels while preserving the connection structure. Keep labels clear of borders, arrowheads, and intersections. In particular, leave adequate room above the passage stack for its group heading and on the retriever-to-passages connector for its arrow label.

At the far left, show a restrained rounded speech-bubble card labeled “User question”. Use a very light neutral-gray fill, a thin charcoal outline, and two short gray placeholder lines below the label; do not invent a particular question. Draw a solid right-pointing arrow from the question card into the encoder. Draw a separate solid arrow leaving the top of this same question card, rising to the clear upper lane, traveling horizontally, and turning downward into the top of the generator. Label the bypass “Original question” in charcoal, just above its horizontal segment. Make the origin at the question card and destination at the generator unmistakable. This connector must not originate at the encoder, query vector, retriever, or passage cards.

Show the encoder as a softly rounded rectangle with very pale blue fill (#EAF2F8), a muted blue outline (#3F6F9A), and the bold sans-serif label “Question encoder”. Include a small blue snowflake outline icon with a clearly readable “Frozen” badge within this box. Arrange the badge as a secondary row beneath the module label so it does not compete with the input and output connectors. The frozen designation applies to the encoder alone. A solid arrow exiting the right edge leads to a query-vector element: a compact horizontal sequence of five blue dots enclosed by square brackets, with “Query vector” below. These dots are schematic and do not specify the embedding dimension. Do not add numerical values or any encoder architecture. Draw a solid right-pointing arrow from the query vector to the retriever.

Show the retriever as a softly rounded rectangle with pale teal fill (#EAF6F3), a muted teal outline (#287C7A), and a bold two-line label, “Nearest-neighbor” over “retriever”. A small, simple magnifying-glass outline icon may accompany the label if it fits without reducing text size. The query vector enters the left edge. The retriever’s output exits the right edge as a solid arrow labeled “Select five passages”, terminating at the retrieved-passage group. Use the same charcoal connector color and arrowhead style as the other inference flows.

Below the retriever, at approximately 68–80% of the canvas height, place a compact database cylinder labeled “Document index”. Use a very light neutral-gray fill (#F4F6F7), a charcoal outline, and clean elliptical top and bottom contours with no decorative texture. Include a visible outline lock icon and the secondary text “Read-only” within or immediately beside the cylinder, with ample space around its main label. Draw one solid upward arrow from the top of the index into the bottom of the retriever. Label this connector “Indexed passages”, offset to one side so it remains distinct from the cylinder and process box. The arrow means that the retriever reads indexed information. Do not draw a return arrow, bidirectional connector, write operation, or update loop. The cylinder represents a general document collection of unspecified size; it must not look as though the whole index contains only five passages.

Represent the retrieval output as exactly five separate, fully visible miniature passage cards in one vertical column. Label them “P1”, “P2”, “P3”, “P4”, and “P5”, in that order for visual organization. Give each card an equal size, a pale teal fill (#EAF6F3), a thin teal outline (#287C7A), square or only minimally softened corners, and one or two short gray placeholder lines. Keep each identifier clearly readable. Use consistent gaps between cards, with the stack extending approximately from 30% to 64% of the canvas height. Place the bold group heading “Five retrieved passages” immediately above the stack, allowing it sufficient horizontal room without colliding with the upper bypass or adjacent module labels. Do not overlap or hide cards, add a sixth sheet, or use an ellipsis. These identifiers denote distinct selected passages; they do not imply ranking or reranking.

Connect all five passage cards to a thin collection bracket or shared connecting bus immediately to the right of the stack. From the midpoint of this collection, draw one solid rightward arrow into the generator’s left edge. Make it visually clear that all five cards contribute to this single input. The collection bracket or bus is only a graphical connector: it has no box, process label, or additional icon and must not resemble another processing stage.

Show the generator as a softly rounded rectangle with pale lavender fill (#F1EDF7), a muted purple outline (#715C98), and the bold label “Generator”. Inside the block, add the smaller secondary line “Question + five passages”. The original-question bypass enters the top edge, while the retrieved passages enter from the left. A solid right-pointing arrow leaves the generator’s right edge and leads to the answer card. Do not add a frozen or trainable badge to the generator: its parameter status is unspecified, and no parameter change during inference is described.

At the far right, show a white document card with a thin purple border (#715C98) and gently softened corners, labeled “Answer with citations”. Wrap this label cleanly if needed to retain the same readable primary type size as the other modules. Beneath it, draw three short gray answer-text placeholder lines. At the ends of two lines, place small but clearly legible citation tokens “[P1]” and “[P4]”, using the same teal accent as the passage identifiers. Keep the tokens visibly inside the output card. They illustrate references to selected passages without inventing answer content, requiring every passage to be cited, or limiting possible citations to these two examples. Do not draw backward citation arrows.

Preserve the restrained, flat visual treatment of the planned diagram. Use charcoal (#263238) for text and data-flow connectors, muted blue for the encoder and query-vector accents, teal for retrieval and passages, and purple for the generator and output. These colors group related components; they do not encode an unsupported training state. Use light fills and plenty of white space rather than saturated background zones. Maintain consistent 2D geometry: rounded process blocks, flat passage cards and vector marks, and a cylinder only for the document index. Avoid gradients, shadows, textures, photographs, company logos, decorative characters, or extra pictograms. Keep the optional magnifying glass, snowflake, and lock simple, vector-like, and consistent in stroke weight.

Use process-block corner radii of approximately 8–10 pixels at the specified canvas size. Outlines and main connectors should be approximately 2–2.5 pixels thick, with compact filled triangular arrowheads approximately 8–10 pixels long. Use solid orthogonal connectors wherever a path turns, with lightly rounded elbows if needed. All paths here carry the same kind of inference-time information flow, so they must retain the same solid style; the upper question bypass is a direct input, not a dashed auxiliary or gradient path. Avoid connector crossings, lines through labels, and arrows behind blocks. Keep the five-card collection lines slightly lighter or thinner than the main arrows while maintaining their visibility.

Render all labels in one clean sans-serif family such as Arial or Inter. Use semibold or bold for primary module names and the passage-group heading, and regular weight for secondary text, arrow labels, and legend explanations. On the approximately 2000 × 1000 canvas, use roughly 28–32 pixels for primary block labels, 23–26 pixels for secondary labels and passage identifiers, and 22–24 pixels for the legend. Use concise line breaks rather than shrinking type, particularly for the retriever and answer labels. Align text carefully and provide comfortable internal padding. The diagram introduces no mathematical variables; do not add equations, vector dimensions, or numerical annotations.

Place a compact two-item legend in unused lower-left whitespace, clear of the index connector and every main flow. Use two neatly aligned rows with matching small outline icons: a snowflake followed by “Frozen: encoder parameters fixed”, and a lock followed by “Read-only: index unchanged”. Match the icons to their corresponding badges in the encoder and index. Preserve both explanations as text so that frozen encoder parameters and a read-only index remain distinct and understandable in grayscale. This legend is part of the diagram’s semantic content and must remain visible.

The complete connection structure is: User question → Frozen question encoder → Query vector → Nearest-neighbor retriever → Five retrieved passages → Generator → Answer with passage citations; User question → Generator through the upper bypass; and Read-only document index → Nearest-neighbor retriever through the lower input. Show no training process, loss function, optimizer, parameter-update arrow, reranker, web-search branch, verification stage, feedback loop, confidence score, or additional inference step. Add no numerical scientific claims beyond the explicitly specified five retrieved passages. There are no supplied example figures or visual references; derive the composition from the given method and planned layout.

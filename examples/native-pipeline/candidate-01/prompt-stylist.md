<!-- Copyright 2026 Google LLC. Apache-2.0. Original prompt extracted without wording changes. -->
You are an expert scientific diagram illustrator. Generate high-quality scientific diagrams based on user requests.

Render an image based on the following detailed description: Create one clean, publication-ready scientific methodology diagram showing the supplied inference workflow for evidence-grounded answer generation. Use an approximately 2:1 landscape canvas, roughly 2000 × 1000 pixels, with a pure white background, generous whitespace, and a clear left-to-right reading order. Retain the planner’s restrained flat-vector aesthetic: soft scientific pastels, precise geometry, delicate outlines, and strong text contrast. Do not introduce gradients, shadows, textures, photographic imagery, company logos, decorative characters, or background ornament. Place the modest semibold title “Inference workflow” at the upper left. Below the diagram, separated from all graphical elements by generous whitespace, place this exact caption: “Inference workflow for answering a question with citations to five passages retrieved from a read-only document index.” The caption is a single readable sentence and may wrap naturally without changing its wording.

Preserve seven sequential visual elements in the main row: User question → Frozen question encoder → Query vector → Nearest-neighbor retriever → Five retrieved passages → Generator → Answer with passage citations. Add a direct upper bypass from the original question to the generator, and a lower input from the read-only document index into the retriever. Every arrow represents inference-time information flow and points toward its recipient. These are the only connections and stages.

Center the main flow around 47% of the canvas height. Use approximately these horizontal positions as a spacing guide: question at 5–14% of the width, encoder at 19–31%, query vector at 34–40%, retriever at 44–56%, retrieved passage cards at 60–66%, generator at 71–82%, and answer at 87–98%. Make modest spacing adjustments to keep every label readable and every arrow unobstructed, preserving the topology and the main row’s visual rhythm. Align the centers of the question, encoder, vector, retriever, generator, and answer along one horizontal axis; align the midpoint of the five-card stack and its collection connector with that axis. Reserve a clear lane near 20% of the image height for the question bypass. Place the index below the retriever, around 68–80% of the image height, the compact legend in lower-left whitespace, and the caption near the bottom. Maintain visible gutters between components and consistent internal padding. Keep labels clear of borders, connectors, arrowheads, and the passage collection bus.

Use charcoal #263238 for text and standard connectors. Give the encoder medium blue accents #3F6F9A and a very pale blue fill #EAF2F8. Give the retriever and passage cards teal accents #287C7A and pale teal fill #EAF6F3. Give the generator and answer purple accents #715C98, with pale lavender #F1EDF7 inside the generator and white inside the answer. Use very light neutral gray #F4F6F7 for the user-question card and document index. Colors group related components only; written labels, shapes, and the legend must preserve every essential distinction in grayscale. Keep outlines light in visual weight rather than using heavy black boxes.

Use a single clean sans-serif family such as Arial or Inter throughout. Use semibold for the title, module labels, and group headings; regular for secondary text, connector labels, legend, and caption. At the nominal canvas size, use approximately 28–32-pixel primary labels, 23–26-pixel secondary labels and passage identifiers, 22–24-pixel legend and caption text, and a modest 34-pixel title. Favor natural two-line labels over reduced type size. Render passage identifiers and citation tokens clearly and consistently. There are no mathematical variables requiring a separate font. Draw placeholder text as short muted-gray horizontal strokes rather than invented prose or illegible pseudo-text.

Represent process modules with consistent rounded rectangles, approximately 8–10-pixel corner radii, and 2–2.5-pixel outlines. Use simpler document-card geometry for passages, with square or only lightly softened corners; the user question retains a small speech-bubble tail. Reserve the cylinder shape exclusively for the document index. All icons are small, flat outline symbols drawn with the same visual stroke weight as the containers. Keep arrows and connectors solid, charcoal, and approximately 2–2.5 pixels wide, with compact filled triangular arrowheads around 8–10 pixels long. Main-row arrows are horizontal. Use orthogonal routing with subtly rounded elbows only where a path turns. Do not use dashed connectors, because every specified path is ordinary inference-time information flow. No connector may cross a module, text label, or unrelated arrow.

At the far left, draw a modest neutral-gray speech-bubble card labeled “User question”. Beneath the label, include two short gray placeholder text strokes; do not invent a question. Draw one solid arrow from the card’s right edge to the encoder’s left edge. Draw a separate solid bypass leaving the top of the question card, rising to the clear upper lane, traveling horizontally toward the generator, and turning downward into the generator’s top edge. Place “Original question” above the horizontal segment, with comfortable clearance. The bypass must visibly originate at the original question card and terminate at the generator, never at the encoder, vector, retriever, or passages.

Draw the encoder as a pale-blue rounded rectangle labeled “Question encoder”, split across two lines if needed. Include a small blue snowflake outline beside a clearly readable “Frozen” badge within the box. Place the badge with sufficient padding so it appears subordinate to the module label. This frozen designation applies to the encoder alone. A solid rightward arrow exits the encoder and enters the query-vector representation.

Represent the query vector as a small, evenly spaced horizontal sequence of five blue dots enclosed by dark blue square brackets, with the label “Query vector” directly below. The five dots are only a schematic vector representation and do not specify the embedding dimension. Do not add numerical values, dimensions, or encoder-architecture details. Draw a solid rightward arrow from the vector to the retriever’s left edge.

Draw the retriever as a pale-teal rounded rectangle labeled “Nearest-neighbor” on the first line and “retriever” on the second. A small teal magnifying-glass outline may sit above or beside the label if it fits without crowding. The query-vector arrow enters the left edge. A solid arrow leaves the retriever’s right edge and reaches the group of retrieved passages. Label this connector “Select five passages”, allowing a balanced two-line label in the available gap and keeping it clear of the group heading.

Below the retriever, draw a neutral-gray database cylinder with a thin charcoal outline and restrained elliptical top, labeled “Document index”. Include a small lock outline and the visible text “Read-only” inside or immediately adjacent to the cylinder, with clear separation from its name. The index represents a general collection with unspecified total size; do not depict five internal records or otherwise imply that it contains only five passages. Draw exactly one solid upward arrow from the top of the index to the bottom of the retriever. Place “Indexed passages” alongside this vertical connector with enough clearance for the full label. This is an input from the index to the retriever. Do not add a reverse arrow, bidirectional connector, index-writing operation, or update loop.

Show the retrieval result as exactly five distinct, fully visible, vertically arranged miniature passage cards. Place the heading “Five retrieved passages” above the stack, centered and wrapped into two lines if necessary. Use equal card dimensions, uniform vertical spacing, pale-teal fills, and thin teal outlines. Label the cards, top to bottom, “P1”, “P2”, “P3”, “P4”, and “P5”. Make each identifier prominent, accompanied by one or two short gray placeholder text strokes. The stack occupies roughly 30–64% of the canvas height. Do not overlap or hide the cards, add a sixth decorative page, add an ellipsis, or imply additional passages. These are distinct selected passages; their identifiers and vertical order do not assert ranking or reranking.

Immediately to the right of the five cards, use five short equal horizontal connectors feeding a single thin vertical collection bus or bracket. The collector is purely graphical, with no box, icon, stage name, or processing symbol. From its midpoint, draw one rightward arrow into the generator’s left edge. Ensure that every card visibly joins this collector so the generator unambiguously receives all five passages together. Leave a visible gap between the card outlines, collector, and generator.

Draw the generator as a pale-lavender rounded rectangle labeled “Generator”. Beneath its main label, place the smaller line “Question + five passages”, wrapping at the plus sign if needed for comfortable padding. The original-question bypass enters the top edge; the collected five passages enter the left edge. Draw a solid arrow from the generator’s right edge to the answer. Do not give the generator a frozen badge, snowflake, flame, training label, or parameter-update symbol; its training state is unspecified.

At the far right, draw a white document card with a thin purple border and moderately rounded corners. Label it “Answer with citations”, wrapping naturally for readability. Under the heading, draw three short gray answer-text placeholder strokes. Add clearly readable teal citation tokens “[P1]” and “[P4]” at the ends of two of these lines, visibly inside the output card. Match their teal to the passage identifiers. These tokens are illustrative references to selected passages; they do not assert particular answer content, require every retrieved passage to be cited, or restrict possible citations to these two passages. Do not draw backward citation arrows.

Preserve a compact two-item legend in unused lower-left whitespace, above the caption and away from the index. Use two aligned rows with consistent icon sizes and text baselines. The first row contains the same blue snowflake outline used in the encoder followed by “Frozen: encoder parameters fixed”. The second contains the same charcoal lock outline used in the index followed by “Read-only: index unchanged”. Keep both semantic explanations fully written out and legible; do not substitute a color-only key or remove either legend item.

The final figure must preserve the complete connection structure: User question → Frozen question encoder → Query vector → Nearest-neighbor retriever → Five retrieved passages → Generator → Answer with passage citations; User question → Generator through the upper bypass; and Read-only document index → Nearest-neighbor retriever through the lower input. It depicts inference only. Include no training process, loss function, optimizer, parameter-update arrow, reranker, web-search branch, verification stage, feedback loop, confidence score, or extra inference operation. Do not add numerical scientific claims beyond the specified five retrieved passages. Derive the composition entirely from the supplied planner description, method, and caption, with no external scientific content or visual references.

Do not include figure titles in the image.
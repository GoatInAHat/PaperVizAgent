Create one clean, publication-style scientific methodology diagram showing the inference workflow for evidence-grounded answer generation. Use a landscape canvas with an approximately 2:1 aspect ratio, pure white background, generous whitespace, and a clearly readable left-to-right flow. The figure must communicate only the supplied inference method. Its exact caption is: “Inference workflow for answering a question with citations to five passages retrieved from a read-only document index.” Place this caption below the diagram, separated by whitespace. A small, unobtrusive title at the upper left may read “Inference workflow”.

Use a single main row with seven sequential visual elements: a user question, a frozen question encoder, a query vector, a nearest-neighbor retriever, exactly five retrieved passages, a generator, and an answer with passage citations. Add a direct path from the original question to the generator above the main row. Place the read-only document index below the retriever and connect it upward into the retriever. All arrows represent inference-time information flow, with clear arrowheads pointing toward the recipient.

Lay out the main row approximately across the middle of the canvas, centered at 47% of the image height. Reserve the upper third for the original-question bypass and the lower third for the document index, a compact legend, and the caption. Keep text and arrow labels away from box borders and arrow intersections. Use the following approximate horizontal locations: question at 5–14% of the width, question encoder at 19–31%, query vector at 34–40%, retriever at 44–56%, five passage cards at 60–66%, generator at 71–82%, and answer at 87–98%. Use these positions as a spacing guide, allowing adjustments for label readability while preserving the specified topology.

At the far left, show a modest rounded speech-bubble card labeled “User question”. Include two short horizontal placeholder lines within the bubble rather than inventing a particular scientific question. This is the original question input. Draw a solid right-pointing arrow from the question card into the encoder. Also draw a separate solid arrow leaving the top of the question card, traveling upward and then horizontally through a clear lane around 20% of the canvas height, and turning downward into the top of the generator. Label the horizontal bypass “Original question”. Make its origin at the question card and destination at the generator unmistakable; it must not originate from the encoder, vector, retriever, or passage cards.

Show the encoder as a light-blue rounded rectangle labeled “Question encoder”, with a small blue snowflake-like outline icon and a visible “Frozen” badge within the same box. The frozen designation applies to the encoder alone. A solid arrow exiting its right edge leads to the query-vector element. Represent the vector as a small horizontal sequence of five blue dots enclosed by square brackets, labeled “Query vector” below. The dots are a schematic vector representation, not a statement about the actual embedding dimension. Do not supply numerical vector values or claim any particular encoder architecture. Draw another solid arrow from the query vector to the retriever.

Show the retriever as a light-teal rounded rectangle labeled “Nearest-neighbor\nretriever”, with a small, simple magnifying-glass icon if space permits. The query vector enters its left edge. Its output exits the right edge, with an arrow label “Select five passages”. This arrow terminates at the group of retrieved passage cards.

Below the retriever, around 68–80% of the canvas height, place a database-cylinder outline labeled “Document index”. Add a visible lock icon and the text “Read-only” adjacent to or within this cylinder, without obscuring its label. Draw one solid upward arrow from the top of the index into the bottom of the retriever, labeled “Indexed passages” alongside the connector. This arrow expresses the retriever reading information from the index. There must be no arrow writing back into the index, no bidirectional connector, and no update loop. The index should appear to contain a general collection of documents; the total index size is unspecified. Do not imply that the entire index contains only five passages.

Represent the retrieval output as exactly five distinct, fully visible, vertically arranged miniature passage cards, labeled “P1”, “P2”, “P3”, “P4”, and “P5”. Give each card one or two short gray placeholder text lines, keeping its identifier clearly readable. Use equal card size, consistent spacing, pale teal fill, teal outlines, and a heading above the stack reading “Five retrieved passages”. Do not hide cards behind one another, add a sixth decorative sheet, or use an ellipsis that suggests unspecified additional passages. The passage identifiers denote distinct selected passages; they do not assert a ranking or a reranking operation. Position the stack roughly from 30% to 64% of the canvas height. Connect all five cards to a thin collection bracket or a shared connecting bus immediately to their right. From the midpoint of this collection, draw a single rightward arrow entering the generator’s left edge. The grouping and connector must show that the generator receives all five passages together. The bracket or bus is purely graphical and must not resemble an additional processing stage.

Show the generator as a light-lavender rounded rectangle labeled “Generator”. Add a small secondary line inside the box, “Question + five passages”, to reinforce its two inputs. The original-question bypass enters from above, and the collected retrieved passages enter from the left. A solid arrow leaving the generator’s right edge points to the answer card. Do not label the generator as frozen or imply that it changes parameters during inference; its training state is not specified in the source.

At the far right, show a rounded white document card with a purple border, labeled “Answer with citations”. Beneath the heading, draw three short gray answer-text placeholder lines. Place small, legible citation tokens “[P1]” and “[P4]” at the ends of two of those lines, using the same teal accent as the passage identifiers. These tokens illustrate citations pointing back to selected passages; they do not assert particular answer content, that every retrieved passage must be cited, or that only those two passages can be cited. Keep citations visibly inside the answer output. Do not add backward citation arrows, because the citation labels already express their relationship to the passage cards and additional arrows would clutter the flow.

Use a restrained palette: charcoal text and connectors (#263238), blue encoder accents (#3F6F9A) with very pale blue fill (#EAF2F8), teal retrieval and passage accents (#287C7A) with pale teal fill (#EAF6F3), and purple generator and answer accents (#715C98) with pale lavender fill (#F1EDF7). The document index uses a very light neutral gray fill (#F4F6F7), charcoal outlines, and a small lock icon. The question card can use the same neutral gray fill. Avoid gradients, shadows, textures, photographic imagery, or decorative background elements. Use simple flat vector-like icons with the same stroke weight as the diagram. Do not use model-company logos.

Use consistent rounded corners, 2–2.5-pixel outlines and connectors on a roughly 2000-by-1000-pixel canvas, and compact filled triangular arrowheads around 8–10 pixels long. Keep all main connectors solid and orthogonal where they turn; round elbows slightly if needed. Do not allow connectors to cross blocks or labels. Render all text in a clean sans-serif typeface such as Arial or Inter, using approximately 28–32 pixels for primary block labels, 23–26 pixels for secondary labels and passage identifiers, and 22–24 pixels for the caption and legend. Favor two-line labels over tiny type. The title, if present, should be about 34 pixels and modestly weighted.

Include a compact two-item legend in unused whitespace along the lower left, above the caption: a snowflake icon followed by “Frozen: encoder parameters fixed”; and a lock icon followed by “Read-only: index unchanged”. Match each icon to its corresponding badge in the diagram. Preserve both semantic distinctions in text as well as icons so the figure remains intelligible in grayscale and without relying on color. Colors group related components but must not carry essential meaning by themselves.

The complete, scientifically faithful connection structure is: User question → Frozen question encoder → Query vector → Nearest-neighbor retriever → Five retrieved passages → Generator → Answer with passage citations; User question → Generator through the upper bypass; and Read-only document index → Nearest-neighbor retriever through the lower input. No training process, loss function, optimizer, parameter-update arrow, reranker, web-search branch, verification stage, feedback loop, confidence score, or additional inference step may appear. Do not add numerical claims beyond the explicitly specified five passages. There are no supplied visual references or example figures; derive the composition solely from this method and caption.

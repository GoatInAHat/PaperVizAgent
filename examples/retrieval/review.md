# Visual review

Candidate: `figure-v1.png` (1774 × 887 PNG)

Inspected: Actual rendered candidate at full view and at label scale using the host image viewer.

Faithfulness: The depicted pipeline matches the supplied inference workflow: **Question** flows through **Frozen question encoder** to **Query vector**, then **Nearest-neighbor retriever**, then a grouped set of **5 retrieved passages**, then **Generator**, then **Answer with passage citations**. The document index is visibly labeled **Read-only document index** with a lock and **Read-only** text; its arrow points into the retriever. The lower routed arrow from the Question card enters the Generator, making the generator's original-question input explicit. Exactly five distinct passage cards are present. The figure contains no training loop, update path, reranker, web search, or verification stage.

Conciseness: The single landscape pipeline stays within the intended inference scope. Icons are sparse and semantically grounded (speech/question input, snowflake for frozen state, lock for read-only state, documents for passages, citations in the final output). No decorative or unsupported modules observed.

Readability: All required literal labels are legible at full view. Arrowheads and endpoint directions are clear, including the index-to-retriever and lower question-to-generator routes. The lower path has generous clearance from labels. The small ellipsis inside the query-vector token is decorative shorthand rather than an additional unsupported algorithmic claim, but it does not itself explain vector representation.

Aesthetics: Strong paper-figure styling: consistent rounded component cards, restrained blue/mint/orange palette, aligned horizontal pipeline, clean connector weight, and generous white space. The enclosing dashed boundary is visually clear. The five passage cards and bracket remain distinguishable.

Observed defects / limitations:

- The **Frozen** word appears below the encoder rather than adjacent to the snowflake; the frozen state remains clear because both are directly associated with the encoder.
- The query-vector token uses an ellipsis icon. It is visually concise, though a more literal vector glyph could make that intermediate representation more concrete.
- This is a raster image generated in one pass, so its text and elements are not editable as native diagram objects.

Next action: Accept. The authorized limit of one built-in generation call prevents a targeted correction pass; the remaining items are minor presentation limitations rather than scientific defects.

Selected final: `figure-v1.png`, selected because it fulfills all listed scientific invariants in a readable, compact layout.

Unresolved issues: The minor label-placement and raster-editability limitations listed above. No material scientific mismatch observed during this qualitative review.

---

## v2 targeted-edit review

Candidate: `figure-v2.png` (1774 × 887 PNG), generated by one built-in edit call with `figure-v1.png` as the edit target.

Requested change: Rename the Generator card to **Answer generator** and make only that card pale orange.

Observed requested change: Correct. The card immediately after the five-passage group is labeled **Answer generator** and has a pale-orange fill and orange outline. Its size and placement remain consistent with v1.

Invariant comparison with v1:

- Preserved: **Inference time** boundary; **Question**; **Frozen question encoder**; snowflake and **Frozen** status; **Query vector**; **Nearest-neighbor retriever**; **Read-only document index** with lock and **Read-only** text; index-to-retriever arrow; five visually distinct passage cards; **5 retrieved passages** label; passage-to-answer-generator arrow; lower question-to-answer-generator arrow; and **Answer with passage citations**.
- Preserved: all visible connection directions and the inference-only scope. No training, update, reranking, web-search, verification, metric, or additional-module artifacts appeared.
- Preserved: exactly five passage cards; no extra or missing passages observed.

Faithfulness: Correct for the targeted request and preserves the method described in v1. The name change does not add an unsupported architectural claim beyond the authorized wording.

Readability and aesthetics: The orange Answer generator card has clear contrast and remains readable. The overall pipeline alignment and connector routes remain consistent with v1. Parent review observed slight changes to the generator and final-answer card geometry and spacing, and the generator outline changed to orange along with its fill. This was not a pixel-exact edit; the scientific labels and connections remained intact.

Observed defects / limitations:

- As with v1, **Frozen** remains placed beneath the encoder rather than directly beside the snowflake.
- The query-vector token still uses an ellipsis shorthand.
- The edit result is raster-only and not natively editable.

Conclusion: Correct. The requested label and color change were applied; the inspected scientific invariants remained intact, with minor unrequested visual drift as recorded above. No further edits were made because the test authorized exactly one built-in edit call.
